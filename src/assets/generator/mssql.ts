import { Context } from '@azure/functions';

export default class MSSQLGenerator {
    // Class variables
    private ctx: Context;

    // Constructor
    constructor (ctx: Context) {
        this.ctx = ctx;
    }

    // Methods
}