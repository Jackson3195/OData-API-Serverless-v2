import { GetFreshContext, MockContext } from '@assets/tests';

describe('MSSQL Generator Service', () => {
    let ctx: MockContext;

    beforeEach(() => {
        ctx = GetFreshContext();
    });


});